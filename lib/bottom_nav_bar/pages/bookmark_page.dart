import 'package:flutter/material.dart';

class BookarkPage extends StatelessWidget {
  const BookarkPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bookark Page'),
    );
  }
}
